function calcularArea() {
    const base = Number(prompt("Ingrese la base (en cm):"))
    const altura = Number(prompt("Ingrese la altura (en cm):"));

    alert("Su área (en cm) es: " + (base + altura)/2);
}